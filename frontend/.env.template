# ==========================================
# Frontend Configuration (Vite)
# ==========================================

# Base URL for the Backend API.
# In production (with Nginx), leaving this as "/api" is perfect.
# In local development, you might change this to "http://localhost:8080/api" if you aren't using a proxy.
VITE_API_BASE_URL=/api
